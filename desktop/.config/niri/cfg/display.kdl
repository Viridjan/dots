// ────────────── Output Configuration ──────────────
/*
You can configure outputs by their name, which you can find
by running `niri msg outputs` while inside a niri instance.
The built-in laptop monitor is usually called "eDP-1".
Find more information on the wiki:
https://niri-wm.github.io/niri/Configuration:-Outputs
Remember to uncomment the node by removing "/-"!

IMPORTANT (corrected 2026-08-25): `config.kdl` does NOT include
`dms/outputs.kdl` at all — this file is the ONLY output source for DP-1.
Earlier docs (2026-08-24) claimed niri does "last-include-wins" merging for
duplicate `output "DP-1" { }` blocks across includes, with this file
(included after dms/outputs.kdl) overriding it. That was WRONG — directly
disproved 2026-08-25 by setting a distinct mode here and confirming via
live `niri msg outputs` that dms/outputs.kdl's value was still what was
actually running. niri appears to take the FIRST occurrence of a duplicate
output block, not the last — so as long as dms/outputs.kdl was included
before this file, everything written here was silently inert, for days,
across multiple "fixes." The only reason changes ever appeared to work
was live `niri msg output ...` CLI overrides (transient, not from this
file) or coincidental mode overlap (165 here happened to match DMS's
165.001, same underlying driver mode). Real fix: removed the
`include "dms/outputs.kdl"` line from config.kdl entirely, so there's no
duplicate block and no merge-order question — this file is unconditionally
authoritative.

165Hz IS the monitor's real native mode (confirmed via journal:
`niri::backend::tty: picking mode: ... vrefresh: 165, mode_type:
ModeTypeFlags(PREFERRED | DRIVER)`, clean clock 878530 — not an
interpolated/synthetic timing) — never the trigger, no reason to avoid it.

VRR IS the trigger — this monitor exhibits luminosity/brightness flicker
under VRR (backlight duty-cycle modulation as frame timing varies; common
FreeSync behavior on some panels, not a driver bug). `on-demand=true` was
tried first (VRR only for windows with `variable-refresh-rate true`) but
appeared to revert to unconditional-enabled on its own — now explained:
that was just dms/outputs.kdl's bare unconditional VRR being what actually
ran the whole time, this file's `on-demand=true` never took effect. VRR is
now off entirely here (property omitted). Tradeoff accepted: no VRR
smoothing for fullscreen games — see steam_app window-rule in config.kdl,
its `variable-refresh-rate true` is now a no-op.

`dms/outputs.kdl` itself is left alone (DMS still writes it, unused now
that it's not included) — don't hand-edit it, it has zero effect either way.
*/

output "DP-1" {

	/* Uncomment this line to disable this output. */
	// off

	/*
	Resolution and, optionally, refresh rate of the output.
	The format is "<width>x<height>" or "<width>x<height>@<refresh rate>".
	If the refresh rate is omitted, niri will pick the highest refresh rate
	for the resolution.
	If the mode is omitted altogether or is invalid, niri will pick one automatically.
	Run `niri msg outputs` while inside a niri instance to list all outputs and their modes.
	*/
	mode "3440x1440@165"

	/* You can use integer or fractional scale, for example use 1.5 for 150% scale. */
	scale 1

	/*
	Variable refresh rate (FreeSync/G-Sync) — DELIBERATELY OMITTED (= off).
	This monitor flickers (luminosity/brightness) under VRR, even on-demand.
	See file header note above (2026-08-25) before re-enabling.
	*/

	/*
	Transform allows to rotate the output counter-clockwise, valid values are:
	normal, 90, 180, 270, flipped, flipped-90, flipped-180 and flipped-270.
	*/
	transform "normal"

	/*
	Position of the output in the global coordinate space.
	This affects directional monitor actions like "focus-monitor-left", and cursor movement.
	The cursor can only move between directly adjacent outputs.
	Output scale and rotation has to be taken into account for positioning:
	outputs are sized in logical, or scaled, pixels.
	For example, a 3840×2160 output with scale 2.0 will have a logical size of 1920×1080,
	so to put another output directly adjacent to it on the right, set its x to 1920.
	If the position is unset or results in an overlap, the output is instead placed
	automatically.
	*/
    position x=0 y=0

    focus-at-startup
}
